const person = {
    name: 'mehedy',
    address: 'Rajshahi'

}

const keys = Object.keys(person);
const values = Object.values(person);

// person.contact = 'no contact';
// person.name = 'ali';

console.log(keys);
console.log(values);